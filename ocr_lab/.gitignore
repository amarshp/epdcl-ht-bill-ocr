__pycache__/
*.pyc
.pytest_cache/

# transient run artifacts (regenerated by run_full.py). The OCR cache holds bill
# TEXT (PII) and is regenerable from the source PDF — keep it out of git.
outputs/
cache/

# bill page images / crops = PII. NOTE: samples/gt/*.json is the frozen GT and IS tracked.
samples/*.png
samples/misses/

# qc_gemini.py loads GEMINI_API_KEY from an external project .env — never store the key here.
